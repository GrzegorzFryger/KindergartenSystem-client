import { TitleChildMapping } from './title-child-mapping';

describe('TitleChildMapping', () => {
  it('should create an instance', () => {
    expect(new TitleChildMapping()).toBeTruthy();
  });
});
